apiVersion: v1
kind: Service
metadata:
  name: dop-c02-eks-app
  labels:
    app: dop-c02-eks-app
    project: dop-c02-lab
spec:
  type: ClusterIP
  selector:
    app: dop-c02-eks-app
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
